# contrib/

Community-contributed algorithm implementations that have not yet met the
bar for `experimental` status under [algorithms/](../algorithms/). See the
maturity model in [algorithms/README.md](../algorithms/README.md).

The directory is reserved so that a contribution below the `experimental`
bar has somewhere to land rather than being turned away; no contributions
have arrived yet.
